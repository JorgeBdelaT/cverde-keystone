{
  "name": "cverde-keystone",
  "version": "2.0.5",
  "private": true,
  "scripts": {
    "dev": "keystone-next dev",
    "start": "keystone-next start",
    "deploy": "keystone-next build && keystone-next prisma migrate deploy",
    "postinstall": "keystone-next postinstall",
    "migrate": "keystone-next prisma migrate deploy",
    "reset-database": "keystone-next dev --reset-db"
  },
  "dependencies": {
    "@keystone-next/auth": "^26.0.0",
    "@keystone-next/fields": "^10.0.0",
    "@keystone-next/fields-document": "^6.0.1",
    "@keystone-next/keystone": "^19.0.0",
    "@types/dotenv": "^8.2.0",
    "dotenv": "^10.0.0",
    "typescript": "^4.3.2"
  }
}
